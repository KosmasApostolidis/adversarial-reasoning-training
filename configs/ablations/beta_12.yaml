# Ablation: TRADES KL weight — beta = 12 (heavy-robustness emphasis).
#
# Diff vs configs/defenses.yaml: trades.beta_start = beta_end = 12.0
# (no annealing; constant high weight, 2x the upper bound of the
# baseline 6 -> 3 anneal range).
# Pass via `--defenses configs/ablations/beta_12.yaml` to art-train.

pgd:
  eps_schedule:
    - {epoch_range: [1, 2], eps: 0.0078}    # 2/255
    - {epoch_range: [3, 3], eps: 0.0157}    # 4/255
    - {epoch_range: [4, 5], eps: 0.0314}    # 8/255
  default_eps: 0.0157
  alpha_ratio: 0.25
  steps: 7
  refresh_K: 1
  eval_eps: [0.0157, 0.0314]
  eval_steps: 40

trades:
  beta_start: 12.0
  beta_end: 12.0
  temperature: 2.0

pgd_at:
  # no extra knobs

oaat:
  alpha: 0.5
