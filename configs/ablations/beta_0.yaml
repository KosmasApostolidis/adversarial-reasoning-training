# Ablation: TRADES KL weight — beta = 0 (clean-only forcing).
#
# Diff vs configs/defenses.yaml: trades.beta_start = beta_end = 0.0.
# At beta=0 TRADES collapses to plain task CE on the clean branch,
# which provides the lower-bound reference for the beta sweep.
# Pass via `--defenses configs/ablations/beta_0.yaml` to art-train.

_extends: ../defenses.yaml

trades:
  beta_start: 0.0
  beta_end: 0.0
